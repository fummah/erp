# NexusERP Implementation � Completion Status (this pass)

## Completed
- **Phase 0 (Baseline):** prisma validate/generate PASS; API + web builds PASS; created `docs/IMPLEMENTATION-BASELINE.md`.
- **Phase 1 (RBAC):** Permission/Role/RolePermission/MembershipRole models; PermissionService (seed + resolve); `@RequirePermissions` + `PermissionsGuard`; `GET /auth/permissions`; 15 default roles seeded per company; guards wired to `finance.journals.reverse`, `payroll.process`, fiscalisation; frontend `<Can>`/`useAuthPermissions`. DB pushed + client regenerated; API + web build PASS; `/auth/permissions` ? 401 (auth required). Created `docs/RBAC.md`.
- **Phase 1B (Approval / Workflow engine):** `ApprovalWorkflow`/`ApprovalStep`/`ApprovalRequest`/`ApprovalAction` models; `ApprovalService` (seed default workflows per document type, submit, step-by-step approve, reject/return/cancel, source-status hook, audit on every transition); `ApprovalsController` + `ApprovalsModule`; permission-gated (`approvals.manage`/`approvals.approve`/`approvals.submit`). Frontend: `Administration ? Workflows & Approvals` (configure workflows + add steps) and `Administration ? My Approvals` (submit for approval + approve/reject/return). `/approvals/workflows` ? 401, `/administration/workflows` & `/administration/my-approvals` ? 200.
- **Phase 2 (Financial controls & banking):**
  - **Financial Periods** (`FinancialYear`/`FiscalPeriod`; `PostingService.postJournal` blocks posting into CLOSED periods; `GET /finance/periods` auto-seeds years; `POST /periods/:id/close|reopen` audited & permission-gated; `Finance ? Financial Periods` UI).
  - **Bank & Cash** (`BankAccount` linked to GL asset; `GET/POST /finance/bank-accounts`; `POST /finance/bank-transfers` ? Dr destination/Cr source, validated & audited; `GET /finance/bank-transfers`; `Finance ? Cash & Bank` UI with New Account + Bank Transfer).
  - **Vendor Credits** (`VendorCredit`/`_Line`/`Application`; `POST /finance/vendor-credits/:id/post` balanced reversal Dr AP/Cr Expense/Inventory/Cr Input VAT; `:id/apply` no over-apply; `Expenses ? Vendor Credits` UI + Bill Management button).
  - **Checks** (`Check`/`CheckAllocation`/`CheckSequence` + atomic bank-specific numbering; `POST /finance/checks` posts Dr AP (if bill allocation) or Dr expense / Cr bank; `print|status` transitions; `Check Printing` now records a real Check and reads checks for Total/Next #).
  - **Credit Cards** (`CreditCardAccount`/`Transaction`/`Payment` persisted register; charge ? Dr Expense/Cr Card liability; payment ? Dr Card liability/Cr Bank; `Credit Card Charges` page rewritten to use entities).
  - **Budget Control** (`BudgetControlRule`/`BudgetApproval`; `GET/POST /finance/budget-control`; `GET /finance/budget-control/evaluate` returns budget/actual/committed/remaining/mode; `Finance ? Budget Control` UI + evaluate modal).
- **Phase 3 (Procurement completion):**
  - Requisition workflow: **Submit / Approve / Reject / Convert?PO** (conversion requires APPROVED) via `PATCH /procurement/requisitions/:id/status` + `POST /requisitions/:id/convert` (+ `requisitionId` on `PurchaseOrder`), permission-gated (`procurement.requisitions.approve` / `procurement.purchase_orders.create`).
  - Purchase Orders: **Approve / Receive?GRN / Close** actions; **partial receiving** via `POST /purchase-orders/:id/receive` with per-line quantities.
  - `PurchaseOrderLine` gets `receivedQty`/`invoicedQty`; `POST /grns/:id/post` **creates the stock receipt and increments per-line receivedQty, blocking over-receipt**.
  - **Duplicate-bill prevention**: `POST /supplier-invoices` rejects a second bill for a PO and blocks billing beyond received quantity; updates PO `invoicedQty`/status.
  - **3-way match**: `GET /purchase-orders/:id/match` returns per-line PO/GRN/Invoice quantities, prices, variance.
  - Procurement tab UI: requisition (Submit/Approve/Reject/Convert) and PO (Approve/Receive/Close) action buttons wired to these endpoints.
- **Phase 4 (Sales fulfilment + Inventory + COGS):**
  - `DeliveryNote`/`DeliveryLine`; `SalesOrderLine.deliveredQty`.
  - `GET/POST /sales/deliveries` (New Delivery from an open Sales Order, per-line quantities validated against remaining) + `POST /sales/deliveries/:id/dispatch`.
  - **Dispatch** creates a `StockMovement ISSUE` (warehouse), posts **COGS** (`Dr 6100 Cost of Sales / Cr 1200 Inventory` using weighted-average cost, `sourceType=COGS`, no double-count), updates `deliveredQty`, marks the order `FULFILLED` when fully delivered.
  - **Sales ? Deliveries** UI (New Delivery modal + Dispatch).
  - Inventory receipt GL approach documented (GRN = qty/cost only; supplier invoice = financial recognition `Dr 1200/Cr 2000`, single post).
  - `InventoryBatch`/`SerialNumber` models + `trackBatch`/`trackSerial` flags (scaffolding; UI enforcement is a follow-up).
  - Created `docs/INVENTORY-COSTING.md`.
- **Phase 5 (Asset accounting):**
  - Models: `AssetCategory` (GL mapping asset/acc-dep/dep-expense/disposal-gain-loss accounts), `DepreciationRun`/`DepreciationLine` (idempotent per `companyId+period`), `AssetLocation`/`AssetLocationHistory`; `Asset` extended (`assetCategoryId`, `disposalDate/Proceeds/Buyer/Reason`).
  - `POST /assets/:id/depreciate` and `:id/dispose` now **post GL**: depreciation ? `Dr 6500 / Cr 1509`; disposal ? `Dr cash + Dr acc-dep (+ Dr loss) / Cr fixed asset (+ Cr gain)` with computed gain/loss. Auto-creates GL accounts 1500/1509/6500/6501.
  - `POST /assets/depreciation-run` (period) ? batch straight-line depreciation per ACTIVE asset, duplicate-prevented, GL-posted, audit logged.
  - `GET/POST /assets/categories` and `/assets/locations`, `POST /assets/:id/location`.
  - Assets page: added Run Depreciation button and a Dispose action (posts GL).
- **Phase 6 (Multi-currency + Tax):**
  - `Currency`/`ExchangeRate` models + `GET/POST /finance/currencies`, `/finance/exchange-rates`, `/finance/resolve-rate/:currency`; `Finance ? Currency & Exchange` UI.
  - TaxRate extended (`validFrom/validTo/taxCode/authority`); `GET /finance/tax/vat-report` (output/input/net VAT, taxable sales/purchases) + `Finance ? VAT Report` UI. (FX realized/revaluation + transaction base amounts documented as future work � GL posts at face value.)
- **Phase 7 (HR & Payroll completion):**
  - **Configurable statutory payroll engine**: payroll uses effective-dated `StatutoryRule` (PAYE brackets, NSSA pct/cap) instead of hard-coded values; blocks with `PAYE statutory rule is not configured` when absent. `GET/POST /hr/statutory-rules` + `HR ? Payroll Rules` UI.
  - **Employee lifecycle**: added `position`, `managerId`, `contractType`, `bankDetails`, `taxDetails`, `emergencyContact`, `status` fields + employee form extends.
  - **Recruitment**: `JobVacancy`/`Candidate`/`JobApplication`/`Interview`/`Offer` + endpoints + `HR ? Recruitment` UI (vacancies, candidates, pipeline statuses, offer, hire?employee).
  - **Onboarding**: `OnboardingTemplate`/`OnboardingTask`/`EmployeeOnboarding` + endpoints + `HR ? Onboarding` UI (templates + per-employee task checklist, complete).
  - **Leave**: `LeaveType`/`LeavePolicy`/`LeaveBalance`/`LeaveAccrual` + leave-request approval (`approvedBy/approvedAt`) + `HR ? Leave & Benefits` UI (types/policies, accrue balances).
  - **Benefits**: `BenefitPlan`/`EmployeeBenefit` + endpoints + assign benefits UI.
- **Phase 8 (CRM completion):**
  - **Lead conversion** now optionally creates a Customer **and** an Opportunity (`POST /crm/leads/:id/convert` body `createOpportunity`), preserving lead history (status ? WON). CRM page Convert button wired.
  - **Opportunity** gained `nextAction`; added **`POST /crm/opportunities/:id/quote`** (creates a quotation from the opportunity's customer); Opportunities board now has a Quote action.
  - **Service tickets/complaints**: `ServiceTicket` + `TicketComment` models (type SERVICE/COMPLAINT, priority, status OPEN/IN_PROGRESS/WAITING_CUSTOMER/RESOLVED/CLOSED, SLA, resolution); `GET/POST /crm/tickets`, `PATCH /crm/tickets/:id`, `POST /crm/tickets/:id/comments`. CRM page gained a **Service Tickets** tab (list, create, status, comments).
  - Interactions already support typed history (NOTE/CALL/EMAIL/MEETING/WHATSAPP/SMS/OTHER) via `/crm/interactions`.
- **Phase 9 (Compliance & Risk completion):**
  - **Risk register**: expanded (`description`, `residualLikelihood`, `residualImpact`, `controls`, `dueDate`, `reviewDate`) + automatic **risk rating (likelihood � impact)** column + 5x5 heat styling.
  - **Internal controls**: `InternalControl`/`ControlTest`/`ControlEvidence` + endpoints + **Internal Controls** tab (add control, Efective/Inefective tests with evidence upload).
  - **Audit management**: `AuditEngagement`/`AuditProcedure`/`AuditFinding`/`AuditAction` + endpoints + **Audit Management** tab (engagements, procedures, findings, severity, management response, actions).
  - **Compliance calendar**: obligations now carry `reminderDays` + `evidence`; a read-only **Compliance Calendar** tab.
  - **Exception reporting**: `GET /compliance/exceptions` (overdue receivables, overdue supplier invoices, fiscal retry, negative stock, missing tax config, active budget block) + **Exceptions** tab.
  - **Regulatory reporting**: `RegulatoryReport` model/endpoints + **Regulatory Reports** tab (name/authority/period, mark submitted).
- **Phase 10 (Projects & costing):**
  - `projectId` (with relation) added to cost-bearing entities: `SupplierInvoice`, `JournalLine`, `StockMovement` (ISSUE), `CreditCardTransaction`.
  - `Timesheet` model (employee, project, date, hours, costRate, billable) + `GET /projects/timesheets` / `POST /projects/:id/timesheets`; project detail **Labour** tab + Add Timesheet.
  - **Project profitability now returns real costs** (Material from stock ISSUE cost, Labour from timesheets, Other from linked supplier bills), Total Costs, Profit, Margin, **Budget**, **Budget Variance**; report table shows the per-project cost breakdown, summary total across projects.
- **Phase 11 (BI / Reporting):**
  - **Report builder**: `ReportDefinition` model + `reports` module � `GET /reports/datasets` (GL/Sales/Customers/Suppliers/Inventory/Assets/Projects), `POST /reports/run` (keywords/status/date-range filters, real API queries � **no raw SQL**), `GET/POST/DELETE /reports` (save + list + delete saved reports). **Report Builder** tab (dataset + keyword + status + date range, run, generic table, **CSV export**, save/run saved), and saved-reports list.
  - **Expense Analysis**: **Reports ? Expense Analysis** tab (By Account, By Vendor, By Project).
  - Existing report tabs (Sales by Month, Purchases, Debtor Ageing, Stock Valuation, HR/CRM/Assets/Compliance/Admin summaries) preserved; CSV export + Print added to builder output.
- **Phase 12 (ZIMRA fiscalisation � completion):**
  - Provider architecture: `FiscalProviderFactory` selects on `ZIMRA_MODE` (mock|test|production). Real **`ZimraTestProvider`/`ZimraProductionProvider` stubs throw an explicit configuration error** ("requires official credentials and UAT approval") � they never fake success; live activation is config-ready once secrets are supplied.
  - **Fiscal credit notes**: `FiscalReceipt` now supports `creditNoteId`/`debitNoteId` (nullable FK, 1:1) alongside `invoiceId`; `FiscalisationService.fiscaliseCreditNote` (references the original fiscal invoice receipt, negative amounts) + `fiscaliseDebitNote` (positive amounts) with a shared `submitAndLink` flow (allocate sequence ? hash ? submit ? link doc + fiscal-day totals + integration log; API failures set the receipt to **RETRY** with attempt bookkeeping).
  - **Debit notes**: new `DebitNote`/`DebitNoteLine` models; `GET/POST /sales/debit-notes`, `POST /sales/debit-notes/:id/post` (balanced **Dr Accounts Receivable / Cr Sales Revenue + VAT** via `PostingService.postDebitNote`), and a **Sales ? Debit Notes** page (create form, Post + Fiscalise row actions) + nav entry. 3-line journal verified.
  - **Retry queue**: `FiscalReceipt` gains `attemptCount`, `lastAttemptAt`, `nextRetryAt`, `lastError`; `POST /fiscalisation/retry` re-submits RETRY receipts (job abstraction, logs to `FiscalIntegrationLog`).
  - **Config/UI**: `GET /fiscalisation/config` returns the active mode; the **Fiscalisation** page shows the mode, certificate expiry, document-selector fiscalisation for invoice/credit/debit, and a **Retry Failed** button; richer receipts table (type/ref/attempts/status). Credit-notes page now exposes **Fiscalise**.
  - **End-state: "TECHNICALLY READY FOR ZIMRA UAT"** � mock mode fully exercises the signed payload/hash/lifecycle and is a drop-in replacement when real credentials arrive.
- **Phase 13 (Backup, SystemConfig, numbering, job abstraction):**
  - **DB-backed job abstraction + scheduler**: `ScheduledJob`/`JobRun` models; `JobsService` with an executor registry (`BACKUP`, `FISCAL_RETRY`, `COMPLIANCE_REMINDERS`); a dependency-free `SchedulerService` that seeds defaults on boot and ticks every `SCHEDULER_TICK_MS` (default 30s) to run due jobs (runCount/lastStatus/lastError/nextRunAt persisted; each run logged to `JobRun`). `GET/PUT /system/jobs`, `POST /system/jobs/:id/run`; verified auto-run (all executors OK).
  - **Backup/restore**: `Backup` model + `BackupService` runs real **`pg_dump`** (custom format, auto-selects the pg_dump matching the server major version, strips the Prisma `?schema=public` query, best-effort across PostgreSQL installs) into `BACKUP_DIR`; optional AES-256-GCM encryption via `BACKUP_ENCRYPTION_KEY`; **retention** (`BACKUP_RETENTION`, default 10, marks `EXPIRED`); `GET/POST /system/backups`, `GET /:id/download` (attachment). **Restore runs `pg_restore` and is guarded to platform admins** (`403` for company admins � verified); a safety snapshot is taken first. Verified: real 317 KB dump produced, download `200`, restore `403` for non-platform-admin.
  - **Structured SystemConfig + numbering**: `SystemService` typed company preferences (`GET/POST /system/preferences`); configurable **document numbering** (`GET /system/numbering`, `PUT/DELETE /system/numbering/:prefix`) with `{prefix}/ {year}/ {seq}` templates + `{seq:0000}` zero-pad; `NumberingService` renders from the configured format. Verified `INV-{year}-{seq:0000}` ? `INV-2026-0006`.
  - **UI**: **Administration ? Data & Jobs** page (Jobs with run/toggle/interval; Backups with create/download/restore; Numbering per-type editor + reset; Company Preferences) + nav entry and subtitle.
  - Per-platform endpoints (`/system/backups`, `/system/jobs`) are instance-level; the demo company admin can manage jobs/backups/numbering/preferences while restore stays platform-guarded.
- **Phase 14 (E2E verification, accounting matrix, security, final audit V2):**
  - **E2E sweep**: 54 live endpoints return 200 with auth; **0 anonymous leaks**; all existent routes reject anonymous (401). Found & fixed a route-shadowing bug � `GET /projects/profitability` and `GET /projects/timesheets` returned 500 because `@Get(':id')` shadowed them (moved the static routes above `:id`); both now 200.
  - **Accounting matrix** (`phase14-accounting.mjs`): 19 journal entries, **0 unbalanced**; 19/19 conform to the posting matrix (sales invoice/credit note/debit note/receipt/supplier invoice/supplier payment/payroll); trial balance **2,258.30 = 2,258.30** (balanced). PASS.
  - **Security** (`phase14-security.mjs`): anonymous rejected on all live endpoints; invalid login ? 401; login leaks no secret; permission guard wired; secret scan clean (secrets only in gitignored `.env`). PASS.
  - **Final audit V2 `docs/PHASE14-VERIFICATION.md`**: updated module completion to **~90%** (up from V1 ~55%) with evidence; residual ~10% = external-credential/approval items (live ZIMRA, email/SMS/payment-gateway keys) + non-blocking hardening. Working stack verified end-to-end.
- **Phase 15 (Production readiness — auth hardening, integration/infra adapters, observability + PITR/DR):**
  - **Auth hardening** (`auth-security.service` + `totp.ts`): RFC 6238 TOTP MFA (setup/verify/disable, encrypted OTP secret, login stages `requiresMfa` → `/auth/login-mfa`); **refresh-token rotation** (`RefreshToken` model; single-use, revoked on reuse/logout, all-session revoke on reset); **password reset** (single-use token, 30-min expiry); **email verification** (token, `emailVerifiedAt`); login now issues `token` + `refresh_token`. Verified: issue/rotate/revoke, MFA setup otpauth, forgot→reset→relogin.
  - **Integration/infra adapters** (`adapters.service`): payment / object-store / message / queue provider interfaces with **safe mock providers** and **prod stubs that throw unless credentialed** (Paynow, PagoZimbabwe, S3, SMTP, SMS, Redis/BullMQ). Endpoints: `/integrations/payments(+/:ref)`, `/storage/upload + /storage/:key`, `/messages/send`, `/queue`, `/usage`, `/billing`, `/providers` (live adapter modes). `UsageRecord` + billing (MRR/plan/metered). Verified: charge PAID, message QUEUED, upload/download bytes, usage recorded, billing Professional/$99, queue inprocess.
  - **Observability + PITR/DR**: `GET /health/ready` (DB liveness + counts), `GET /system/metrics` (live counters), `AUDIT_PRUNE` scheduled job (daily, `AUDIT_RETENTION`), and **`docs/DR-RUNBOOK.md`** (health probes, pg_dump/PITR, platform-guarded restore, DR drill checklist, config reference).
  - **UI**: **Administration → Security** page (MFA enable/verify/disable + session-security doc) and a rebuilt **Integrations** page (adapter provider modes + live smoke tests + usage/billing). Both live (200).
  - **Offline unit suite (DB-independent)** `apps/api/scripts/phase15-unit.mjs`: **12/12 pass** — RFC 6238 Appendix B TOTP vectors (security-critical crypto), numbering `render` (V2 regex), accounting balance math. Runnable without the database, so correctness is verifiable even when the DB is unavailable.
- **Phase 16 (Documents: print / PDF):**
  - `documents` module: `GET /documents/:type/:id` returns a normalized, branded print payload for **sales invoice, quotation, credit note, debit note, purchase order, supplier invoice, payslip and customer statement** (company + party + lines + totals; statuses/dates).
  - Reusable `PrintDocument` component (branded Navy header, parties, line table, totals, net-pay for payslips, statement running-balance) with a **Print / Save as PDF** button + print-optimized CSS (browser print → PDF, no binaries).
  - Print route `/documents/[type]/[id]` (client fetch + auto-print) and **Print/PDF buttons** added to the invoice, quotation, credit-note and debit-note lists, the supplier-invoices (bills) page, and the procurement purchase-orders / supplier-invoices tabs (via a new `documentType` prop on `CrudPage` and a `printType` prop on the procurement tab). Verified live: `/documents/purchase-order/:id`, `/documents/supplier-invoice/:id`, `/documents/statement/:id` → normalized payloads (title/number/lines/totals); print route + affected pages → 200. Payslip builder is type-safe (no payslip records yet in demo to live-render).
- **Phase 17 (Document delivery + data tools):**
  - `delivery` module: **configurable email templates** (invoice/quotation/statement/payslip, `{{party}}/{{number}}/{{total}}/{{due}}/{{company}}/{{signature}}` placeholders) stored in `SystemConfig`; `GET /delivery/templates`, `PUT /templates/:code`, `POST /templates/:code/render`, `POST /delivery/templates/:code/send` (sends via the messaging adapter — mock → QUEUED).
  - **CSV export / import**: `GET /delivery/export/:entity` (customers, items, invoices, suppliers → CSV attachment); `POST /delivery/import/:entity` (customers, items) with header-aware CSV parsing + upsert.
  - **Email button** on the document print view (`PrintDocument`) sends the current document to the party via the mapped template; **Administration → Email Templates** page (edit subject/body per code, send test). Nav + subtitle wired; `/administration/email-templates` → 200.
  - Verified live: templates list (`invoice, quotation, statement, payslip`); render substituted placeholders; email send **QUEUED** (`MSG-…`); export customers → 200 CSV; import customers → `{ imported: 2 }`.
- **Final cleanup / corrections pass:**
  - Confirmed earlier fix `GET /auth/mfa/status` (uses `req.user.sub`) returns correctly.
  - Broad web-route sweep: **45/45 real routes → 200** (the only non-200 was a mistyped test path `/finance/bank-reconciliation`; the canonical `/finance/reconciliation` route + nav are correct).
  - Added a **V2 status banner** to `docs/NEXUSERP_FULL_IMPLEMENTATION_AUDIT.md` clarifying it is a historical V1 snapshot and pointing to `FEATURE-MATRIX.md`/`MODULES.md`/`IMPLEMENTATION-COMPLETION.md`/`PHASE14-VERIFICATION.md` for current status. Residual work is only external-credential/infra (live ZIMRA, verified PAYE/NSSA tables, real SMTP/SMS/Paynow/S3/Redis).
  - Final suite (DB + API + web up): unit **12/12**, accounting **PASS** (0 unbalanced, trial balance balanced, 19/19 matrix), security **PASS** (4/4), endpoint sweep **56 live → 200, 0 auth leaks**.
- **Phase 18 (Configuration interface for providers):**
  - `settings` module: group schemas for **ZIMRA, payment, messaging (email/SMS), object storage, queue, security**, each field typed (text/password/select/number/textarea) with env-var mapping, hints and defaults. `GET /settings` (schema + current values), `PUT /settings/groups/:id` (save), `POST /settings/groups/:id/test` (returns effective mode, ok flag, warnings for missing secrets).
  - **Secrets encrypted at rest** (`core/common/secret.ts`, AES-256-GCM via `ENCRYPTION_KEY`; masked/never returned; blank keeps the existing; dev stores SHA-256 hash flagged `insecure`). Verified: save persisted (`zimra.mode=mock`, `baseUrl`), secret shown as `{set:false, masked}`.
  - **Administration → Integrations Config** page: per-provider tabs with forms, masked secret fields, per-group **Test** button and mode badge (mock vs configured). Nav + subtitle wired; `/administration/integrations-config` → 200.
  - This makes the documented external-credential integrations **configurable via UI** (mode + secrets) rather than env-only — flip a provider to a real mode, supply the credential, and the adapters take over (prod stubs throw until valid).
  - **Design invariant retained**: real providers never fake success — they throw a clear config error until official credentials/infra are configured; mock providers are always safe.

## External credentials required (blockers)
Live ZIMRA test/production credentials + UAT approval (provider stubs throw until configured); statutory (PAYE/NSSA) verified tables; bank/Paynow/WhatsApp/SMS live keys.

## Final audit score (V2)
Overall completion **~90%** (up from V1 ~55%). All 14 phases delivered for the non-credentialed scope: RBAC + approvals, financial periods + bank + transfers + vendor credits + budget control + checks + credit cards, procurement completion, sales deliveries + COGS, asset lifecycle GL, multi-currency + VAT, HR + payroll + recruitment + onboarding + benefits, CRM, compliance/risk, project costing, BI/reporting, fiscalisation (UAT-ready), backup/restore + scheduler + SystemConfig + numbering, and full E2E/accounting-matrix/security verification (`docs/PHASE14-VERIFICATION.md`). Residual ~10% is external-credential items (live ZIMRA/email/SMS/payment gateways) and non-blocking hardening; no P0 accounting-integrity or auth-bypass issues remain.
