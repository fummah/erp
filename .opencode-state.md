# Session State — NexusERP (HR: attendance engine + payslip publish/PDF)

## Last task: attendance hours/overtime engine + payslip publish/PDF
Both the prior scope-noted gaps (minus salary RBAC + reports) are now done.

### Attendance engine (backend)
- WorkCalendar extended: scheduledStart, scheduledEnd, breakMinutes. Attendance extended: scheduledStart/End, breakMinutes, workedHours, regularHours, overtimeHours, lateMinutes, earlyDeparture, source, approved, approvedBy.
- HrService.calculateAttendance: derives worked hours (checkOut-checkIn minus break), regular hours (capped at scheduled net length), overtime (above schedule), late minutes (checkIn after scheduled start), early departure (checkOut before scheduled end) — all server-side, no manual entry.
- Endpoints: GET /hr/attendance/exceptions (MISSING_CLOCK_IN/OUT, LATE_ARRIVAL, UNAPPROVED_OVERTIME, EARLY_DEPARTURE, ABSENCE), GET /hr/attendance/summary?from&to (totals + byEmployee), POST /hr/attendance computes on create, PATCH + POST /hr/attendance/:id/approve (RBAC hr.attendance.manage).
- UI: Attendance tab now has summary cards (records/worked/OT/exceptions), exception chips, and columns for In/Out/Worked/Regular/OT/Late/Status/Approved (+ Approve OT button).

### Payslip publish/PDF (backend)
- Endpoints: POST /hr/payslips/:id/publish (GENERATED->PUBLISHED w/ publishedAt), POST /hr/payslips/:id/void (VOID). RBAC payroll.payslips.publish. Audit logged.
- Payslip preview drawer now has Publish + Print/PDF buttons; added @media print CSS to globals.css so only the payslip prints (body hidden, #payslip-print shown).

### Verified end-to-end (admin@demo.local / Password123!)
- Attendance on 08:00-17:00 / 60m break cal: on-time=8h; 08:30-16:30=7h, late 30m, early 30m; 08:00-19:00=worked 10h, regular 8h, OT 2h. Summary aggregates; exceptions detect.
- Payslip: GENERATED -> PUBLISHED (publishedAt set) -> VOID. Full build passes, API health 200.

### Note / gotchas
- globals.css: do NOT use PowerShell Add-Content with non-ASCII; it wrote em-dash as invalid byte 0x97 and broke the build. Fixed by re-encoding to UTF-8 and stripping the BOM (Tailwind requires @import first, no BOM). Use the Write/Edit tools for CSS.
- PAYE rule now configured in DB (from earlier test) so payroll processes.

## Remaining out-of-scope items still not built
- Salary-visibility RBAC gating on the frontend (endpoints ARE guarded; UI still shows salary broadly).
- HR reports screens, PDF export for other docs.
