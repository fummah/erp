'use client'; import { Tabs } from 'antd'; import { DataTablePage } from '@/components/data-table-page'; export default function Finance(){return <Tabs items={[{key:'accounts',label:'Chart of Accounts',children:<DataTablePage title="Financial Management" subtitle="General ledger and control accounts." path="/finance/accounts" columns={[{title:'Code',dataIndex:'code'},{title:'Account',dataIndex:'name'},{title:'Type',dataIndex:'type'}]}/>},{key:'journals',label:'Journals',children:<DataTablePage title="General Journal" subtitle="Posted accounting events." path="/finance/journals" columns={[{title:'Number',dataIndex:'number'},{title:'Date',dataIndex:'date',render:(v:string)=>new Date(v).toLocaleDateString()},{title:'Description',dataIndex:'description'},{title:'Source',dataIndex:'sourceType'}]}/>}]} />}
