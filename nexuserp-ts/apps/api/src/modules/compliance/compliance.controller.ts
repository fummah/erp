import { Controller, Get, Req, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { PrismaService } from '../../core/prisma/prisma.service'; import { JwtAuthGuard } from '../auth/auth.guard'; import { companyIdOf } from '../../core/context';
@ApiTags('Compliance') @ApiBearerAuth() @UseGuards(JwtAuthGuard) @Controller('compliance') export class ComplianceController{constructor(private prisma:PrismaService){} @Get('risks') risks(@Req() req:any){return this.prisma.risk.findMany({where:{companyId:companyIdOf(req.user)}})} @Get('obligations') obligations(@Req() req:any){return this.prisma.complianceObligation.findMany({where:{companyId:companyIdOf(req.user)}})} }
