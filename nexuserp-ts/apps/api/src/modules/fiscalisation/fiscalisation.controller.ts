import { Body, Controller, Get, Param, Post, Req, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { JwtAuthGuard } from '../auth/auth.guard'; import { companyIdOf } from '../../core/context'; import { FiscalisationService } from './fiscalisation.service';
@ApiTags('Fiscalisation') @ApiBearerAuth() @UseGuards(JwtAuthGuard) @Controller('fiscalisation') export class FiscalisationController{constructor(private fiscal:FiscalisationService){} @Get('devices') devices(@Req() req:any){return this.fiscal.listDevices(companyIdOf(req.user))} @Post('devices/:id/register') register(@Req() req:any,@Param('id') id:string){return this.fiscal.register(companyIdOf(req.user),id)} @Post('devices/:id/open-day') open(@Req() req:any,@Param('id') id:string){return this.fiscal.openDay(companyIdOf(req.user),id)} @Post('devices/:id/close-day') close(@Req() req:any,@Param('id') id:string){return this.fiscal.closeDay(companyIdOf(req.user),id)} @Post('devices/:id/fiscalise') fiscalise(@Req() req:any,@Param('id') id:string,@Body() body:{invoiceId:string}){return this.fiscal.fiscalise(companyIdOf(req.user),id,body.invoiceId)} @Get('receipts') receipts(@Req() req:any){return this.fiscal.receipts(companyIdOf(req.user))} }
