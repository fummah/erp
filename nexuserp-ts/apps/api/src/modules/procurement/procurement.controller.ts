import { Controller, Get, Req, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { PrismaService } from '../../core/prisma/prisma.service'; import { JwtAuthGuard } from '../auth/auth.guard'; import { companyIdOf } from '../../core/context';
@ApiTags('Procurement') @ApiBearerAuth() @UseGuards(JwtAuthGuard) @Controller('procurement') export class ProcurementController{constructor(private prisma:PrismaService){} @Get('suppliers') suppliers(@Req() req:any){return this.prisma.supplier.findMany({where:{companyId:companyIdOf(req.user)}})} @Get('purchase-orders') orders(@Req() req:any){return this.prisma.purchaseOrder.findMany({where:{companyId:companyIdOf(req.user)},include:{supplier:true,lines:true}})} }
