import { Controller, Get, Req, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { PrismaService } from '../../core/prisma/prisma.service'; import { JwtAuthGuard } from '../auth/auth.guard'; import { companyIdOf } from '../../core/context';
@ApiTags('HR & Payroll') @ApiBearerAuth() @UseGuards(JwtAuthGuard) @Controller('hr') export class HrController{constructor(private prisma:PrismaService){} @Get('employees') employees(@Req() req:any){return this.prisma.employee.findMany({where:{companyId:companyIdOf(req.user)},include:{department:true}})} @Get('statutory-rules') rules(){return this.prisma.statutoryRule.findMany({where:{active:true},orderBy:{validFrom:'desc'}})} }
