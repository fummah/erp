import { Body, Controller, Get, Param, Post, Req, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { PrismaService } from '../../core/prisma/prisma.service'; import { JwtAuthGuard } from '../auth/auth.guard'; import { companyIdOf } from '../../core/context'; import { CreateInvoiceDto } from './sales.dto'; import { PostingService } from '../finance/posting.service';
@ApiTags('Sales') @ApiBearerAuth() @UseGuards(JwtAuthGuard) @Controller('sales') export class SalesController{constructor(private prisma:PrismaService,private posting:PostingService){}
@Get('customers') customers(@Req() req:any){return this.prisma.customer.findMany({where:{companyId:companyIdOf(req.user)},orderBy:{name:'asc'}})}
@Get('invoices') invoices(@Req() req:any){return this.prisma.salesInvoice.findMany({where:{companyId:companyIdOf(req.user)},include:{customer:true,branch:true,lines:true,fiscalReceipt:true},orderBy:{createdAt:'desc'}})}
@Post('invoices') async create(@Req() req:any,@Body() dto:CreateInvoiceDto){const companyId=companyIdOf(req.user); let subtotal=0,taxTotal=0; const lines=dto.lines.map(l=>{const net=l.quantity*l.unitPrice; const tax=net*(l.taxRate/100); subtotal+=net; taxTotal+=tax; return {...l,taxAmount:Number(tax.toFixed(2)),lineTotal:Number((net+tax).toFixed(2))}}); return this.prisma.salesInvoice.create({data:{companyId,branchId:dto.branchId,customerId:dto.customerId,invoiceNo:dto.invoiceNo,currency:dto.currency||'USD',subtotal:Number(subtotal.toFixed(2)),taxTotal:Number(taxTotal.toFixed(2)),total:Number((subtotal+taxTotal).toFixed(2)),fiscalRequired:dto.fiscalRequired??true,fiscalStatus:(dto.fiscalRequired??true)?'READY':'NOT_REQUIRED',lines:{create:lines}},include:{lines:true}})}
@Post('invoices/:id/post') post(@Req() req:any,@Param('id') id:string){return this.posting.postSalesInvoice(companyIdOf(req.user),id)}
}
